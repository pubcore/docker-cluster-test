{
	"name": "@pubcore/docker-cluster-test",
	"description": "create clusters for testing purpose",
	"author": "pubcore",
	"scripts": {
		"test": "nyc --reporter=text mocha --inspect --recursive",
		"coverage": "nyc report --reporter=text-lcov"
	},
	"main": "js/index.js",
	"license": "MIT",
	"devDependencies": {
		"eslint": "^5.16.0",
		"eslint-plugin-mocha": "^5.3.0",
		"mocha": "^6.1.4",
		"nyc": "^14.1.1",
		"@pubcore/node-docker-build": "^1.6.0"
	},
	"dependencies": {
		"is-port-reachable": "^2.0.1",
		"shelljs": "^0.8.3"
	}
}
